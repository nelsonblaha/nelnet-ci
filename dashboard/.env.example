# GitHub token for API access (recommended)
# Create at: https://github.com/settings/tokens with 'repo' scope
# Without this, workflow status will be limited by rate limits
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Plex integration (optional)
# Set both to enable Plex transcode monitoring
# PLEX_URL=http://host.docker.internal:32400
# PLEX_TOKEN=your_plex_token

# Admin password for dashboard API (optional)
# ADMIN_PASSWORD=your_secure_password
